import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Maxtsh Blog</title>
      </Head>
    </div>
  );
};

export default Home;
